@typeparam TPropertyValueType
@inherits CslaNumericInputBase<TPropertyValueType>

<div>
    @if (ShowLabel)
    {
        <CslaLabel Property="Property" />
    }
    <TelerikNumericTextBox 
        T="TPropertyValueType"
        Id="@Property.PropertyName"
        @bind-Value="BoundValue" 
        @bind-Value:event="@BindEvent"
        Min="@Min"
        Max="@Max"
        Step="@Step"
        Format="@Format"
        Decimals="@Decimals"
        Arrows="@Arrows"
        Enabled="@Property.CanWrite"
        Width="@Width"
        Class="@Class" />
    <CslaInputValidationMessages Property="Property" />
</div>
